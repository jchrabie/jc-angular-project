import { FilteredSearchPipe } from './filtered-search.pipe';

describe('FilteredSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
